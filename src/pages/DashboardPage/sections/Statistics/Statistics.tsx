import { FC } from 'react'
import styles from './Statistics.module.scss'

const Statistics: FC = () => {
	return <section className={styles.statistics}>Statistics</section>
}

export default Statistics
